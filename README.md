# practice-creating-JSON-objects-Paula-B

Goal: Create JSON object representing movie in catelog.

- Title (string),
- Year (number),
- isAvailable (boolean),
- Genre (array),
- Cast (nested objects)
